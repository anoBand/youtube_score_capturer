version: '3.8'

services:
  web:
    build: .
    container_name: youtube-score-capturer
    ports:
      - "5000:5000"
    volumes:
      # [추가] 호스트의 cookies.txt를 컨테이너 내부로 마운트
      - ./cookies.txt:/app/cookies.txt
    restart: always