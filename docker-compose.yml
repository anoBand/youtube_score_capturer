version: '3.8'

services:
  web:
    build: .
    container_name: youtube-score-capturer
    ports:
      - "5000:5000"
#    volumes:
#      - ./cookies.txt:/app/cookies.txt # 쿠키 필요 시
    restart: always